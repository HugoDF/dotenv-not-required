{
  "private": true,
  "type": "module",
  "scripts": {
    "start:dotenv": "node -r dotenv/config index.js",
    "start:native": "node --env-file=.env index.js",
    "test": "node --test",
    "lint": "npm run format -- -c",
    "format": "prettier --write './**/*.{js,json,yml,md}'"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "3.0.3"
  },
  "dependencies": {
    "dotenv": "16.4.5"
  }
}
